# Limitations

The proof is at the Set/coalgebra level claimed by the propositions; it is not
an empirical statement about performance on a large RL benchmark. Proposition
4.2 uses the standard nonempty-MDP extension to unused representation states,
an implicit convention in the source proof. Degenerate empty-state/empty-
observation structures are outside that RL convention and are not claimed.


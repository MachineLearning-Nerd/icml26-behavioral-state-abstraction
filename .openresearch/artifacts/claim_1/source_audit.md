# Source audit

Definition 3.1 quantifies over an arity `n ∈ N` and defines an `n`-ary bundle
over `V` as a set `X` with a map `h_X : X^n -> V`. Section 2 defines an
`F`-coalgebra as a set `X` with `t_X : X -> F X`. Neither statement asserts a
finite state count or benchmark result.

